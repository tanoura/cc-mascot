# ghコマンドを使ってGithubへコメントを投稿する際のルール

ghコマンドを使ってローカルから文章を投稿する場合のルールを示します。
PRやIssueの概要、コメントなどを記載する際に適用されます。

## LLMの生成した文章であることを明記

投稿文の最後に共著であることを下記のように明記してください。
モデル名には自身のLLMモデル名をいれてください。

```markdown
---

Written-By: [LLMモデル名]
```

目的: ghコマンドを使って投稿すると投稿者がユーザー本人になってしまい、実際はLLMが書いたものが区別できなくなってしまうのを防ぐため